# generated: 2026-08-04T18:18:39+0200
# last_updated: 2026-08-20  # story 4.2 -> ready-for-dev (context created)
# project: Quantique
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created.
# - Stories can be worked in parallel if team capacity allows.
# - Developer typically creates the next story after the previous one is 'done'.
# - Dev moves a story to 'review', then runs code review.
#
# ============================================================================
# SPRINT CHANGE (approved 2026-08-05): Pivot to a Phaser guided adventure.
# See planning-artifacts/sprint-change-proposal-2026-08-05.md
# - Epic 1 retitled "Phaser guided-adventure foundation".
# - Domain/store/evaluator/persistence KEPT; DOM presentation layer retired.
# - Prediction & conclusion become 1-of-4 colleague choices; rival-lab critique
#   (no hard fail); "2 significant measures" gate unlocks the conclusion.
# - Bilingual EN+FR from launch (Story 1.1b — build early).
# - Accessibility de-scoped from MVP (Story 7-2 descoped, revisit post-MVP).
#
# NON-STANDARD STATUS VALUES (intentional):
#   - descoped: out of MVP scope per the pivot; not available work (won't be picked up).
#   - skipped: retrospective intentionally not conducted.
# KEY-IDENTITY NOTE: story identity is the epic.story NUMBER. Reworked stories
#   keep their original keys (which match existing story files + git history)
#   even though their epic titles changed; "# rework:pivot" marks the drift.
#   These need re-baselining via gds-create-story before dev.
# ============================================================================
# SPRINT CHANGE (approved 2026-08-06): Playable Phaser surface.
# See planning-artifacts/sprint-change-proposal-2026-08-06.md
# - Epic 2 reopened: Stories 2.7-2.12 added. No story rolled back.
# - Cause: nine of fourteen player intents were dispatchable only from the
#   retired src/ui/* DOM panels, and LibraryScene/DebriefScene were still
#   PhasePlaceholderScene while their owning stories were marked done.
#   New NFR20 (canvas intent completeness) and ADR-011 prevent recurrence.
# - Decisions: coded vector silhouettes for the cast (no art assets); knob drag
#   plus discrete steps; full scope including Debrief and panel retirement.
# - Story 2.4's release gate stays Blocked until 2.12 ships.
# - A story marked done before 2026-08-06 may have met its acceptance criteria
#   without delivering a canvas surface. Check before building on it.
# ============================================================================

generated: 2026-08-04T18:18:39+0200
last_updated: 2026-08-20
project: Quantique
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  epic-1: done  # reopened by pivot (was: done)
  1-1-project-bootstrap-and-verification-harness: done
  1-1b-internationalization-foundation-english-french: done  # new (pivot); BUILD EARLY — right after 1-1, before any scene text
  1-2-minimal-young-case-contract-and-authored-loop: done
  1-3-accessible-dual-surface-laboratory-controls: done  # rework:pivot -> "Phaser laboratory controls"
  1-4-measurement-notebook-and-run-comparison: done  # rework:pivot -> in-scene notebook
  1-5-curated-record-and-source-labels: done  # rework:pivot -> LibraryScene (reuse LectureBookRenderer); superseded-impl: delivered via DOM CuratedRecord, LibraryScene stayed a placeholder -> see 2.8
  1-6-evidence-to-conclusion-theory-board: done  # rework:pivot -> choose 1-of-4 colleague conclusion
  1-7-consultations-peer-review-and-revision-history: done  # rework:pivot -> colleague hints + rival-lab critique
  1-8-offline-progress-export-import-and-print: done  # kept (print/export retained)
  1-9-inclusive-feedback-and-inquiry-recognition: done  # rework:pivot -> in-scene feedback, a11y descoped
  1-10-scene-router-and-adventure-flow: done  # new (pivot)
  1-11-colleague-cast-and-proposal-system: done  # new (pivot)
  1-12-phaser-dialogue-and-choice-ui: done  # new (pivot)
  epic-1-retrospective: skipped
  epic-2: done  # reopened 2026-08-06 by sprint-change-proposal-2026-08-06.md
  2-1-young-contextual-record-and-prediction: done  # rework:pivot -> library reading + choice-of-4 prediction; superseded-impl: delivered via DOM CuratedRecord + LectureBookScene overlay, not LibraryScene -> see 2.7/2.8
  2-2-young-double-slit-experiment: done  # kept (deterministic model reused)
  2-3-young-synthesis-debrief-and-replay: done  # rework:pivot -> significant-measure gate + colleague conclusion; superseded-impl: debrief/replay delivered via DOM HistoricalDebriefPanel, DebriefScene still a placeholder -> see 2.11
  2-4-young-learning-and-educator-validation-gate: done  # rework:pivot -> re-baselined 2026-08-06: metrics revised, EN+FR gate added, a11y gate removed. Instrument delivered and reviewed; the release gate itself stays Blocked and cannot be run until 2.5/2.6 ship
  2-5-rival-lab-critique-and-revision: done  # new (pivot); reviewed 2026-08-06: 7 patches applied, 4 decisions resolved, 7 items deferred
  2-6-significant-measure-gate-and-colleague-hints: done  # new (pivot); implemented + reviewed 2026-08-06 — gate + hints shipped at case.json 1.11.0; its ad-hoc advance control is consolidated into 2.7
  # ---- Added 2026-08-06 (playable-Phaser-surface correction). Build 2.7 FIRST: it
  # ---- delivers the reusable advance affordance 2.8/2.10/2.11 consume. 2.12 STRICTLY LAST.
  2-7-in-scene-phase-transitions: done  # new; unblocks every story below; implemented 2026-08-07 — all six transitions canvas-dispatchable; two intents in the review→debrief chain still unowned (see deferred-work.md) and block 2.12
  2-8-library-reading-room-and-reference-book: done  # new; implemented 2026-08-07, reviewed 2026-08-07 — reading room + book on canvas, source.inspected canvas-dispatchable, LectureBookScene overlay + scene reach-in retired, case.json at 1.13.0 (readingGateHints + reviewed-needs-rendition); review applied 32 patches, deferred 3, assigned the two review→debrief intents to 2.11
  2-9-colleague-and-rival-lab-characters: done  # reviewed 2026-08-07, 25 patches applied. Coded vector figures, no art assets. The review found the conclusion board staging ZERO figures at every panel height (AC1/AC2/AC3 unmet on synthesis and review) and no test able to see it — every staging test fabricated its band. Fixed by freeing the stated limitation from the card: it now shows for the CHOSEN proposal in the guide slot, cards drop 116->84, and both boards stage the full cast through a three-line beat. stage.speaking is now actually drawn (AC2's label); it shipped authored in both locales and rendered by nothing. case.json IS changed (authored `figure` vocabulary) and is now 1.14.0 with the save-compat allowlist extended — the earlier 'no case.json change' note was wrong. New ColleagueGeometry.test.ts drives the real board geometry. Open for 2.10: canvas-transitions exceeds its 30s budget at --workers=9 (passes at 5) now that the conclusion board draws a cast — see deferred-work.md.
  2-10-physical-apparatus-and-player-started-light: done  # reviewed 2026-08-07, 25 patches applied (23 patch + 2 decisions resolved into patches). Three parallel review layers; the two load-bearing findings came from MUTATION TESTING, not reading: AC4's entire painted dark state (`const dark = false`) and AC3's one-step-per-arrow-press (doubled `step()`) each left 982/982 green. Root cause was the test harness — `tests/unit/sceneSlice.ts` swallowed `clear`, every Graphics fill, setInteractive/disableInteractive and removeCapture, and keyed listeners by event name alone — so dark vs lit, locked vs usable, and leaked vs released were all indistinguishable. Also fixed: a restored session ignited a phantom 2.4s run and locked the bench (ADR-012 gated on scene lifecycle, reachable via main.ts restore); a mid-run reduced-motion toggle left the bench PERMANENTLY locked at 1 of 12 controls; the knob's dead zone flipped max->min ~46 deg past the end and slammed to an extreme when a drag slid onto the step affordance (fixed with hysteresis, story owner's call); atan2(0,0)=0 put the knob centre at 83.3% of travel; a refused controlSet desynced the knob with the reverse turn swallowed; the result readout could reach into the instrument readouts and neither growing text block was in the non-overlap sweep; the arrow capture (global in Phaser) was never released; re-choosing 550nm discarded consultation/peerReview/rivalLabCritique and expired both message slots. 9 mutation proofs, each broken and restored. Unit 997 (from 982); e2e 53/7 at 5 workers, same seven retired-DOM names, two identical runs; typecheck clean. NFR1 gate ASSIGNED to Alexis with a non-circular trigger (before 2.12 is done, not before 2.4's gate which is itself blocked on 2.12) and AC10's unsatisfiable 'character staging' clause corrected. apparatus.reset still unowned; assign before 2.12.
  2-11-debrief-scene-and-replay: done  # reviewed 2026-08-07, 29 patches applied (27 patch + 2 from decisions), 3 deferred, 3 dismissed. Three parallel review layers. The story's own work was sound — AC1-AC6 met, scope boundary held, test arithmetic exact (997/61 -> 1125/67 verified against a clean baseline worktree), 8 of 9 claimed mutation rows reproduced. What the review found was ONE defect class, sixteen times: text measured against a reserve that cannot hold it. The case file's reference row reserved 10px for a line that is ~15px at the clamp floor and its observation row one line for a three-part detail that wraps to two - both cropped in EN and FR on every render, no long string needed. Four French conclusion.missing.* lines (AC7's readiness list) measured 383-470px against a 372px column and were cut mid-sentence, two of them on the ordinary path to review. deeperTheory.title (unbounded authored text, no .max() in schema), the recognition status marker, the whole overlay chrome and the debrief refusal were unclamped. NONE of it was visible to 1125 green tests, because sceneSlice.ts reports a constant height:18 for every text object and french-typography swept fixed-height CONTROL LABELS only - the harness gap the dev had already flagged as carried-forward, now the top deferred item. Fixed by deriving every reserve from exported caseFileLineHeight/debriefLineHeight helpers, growing the rows to hold their own content (CASE_FILE_ROWS_PER_PAGE 4->3 pays for it), and shortening both locales rather than relaxing a bound. Also: the two clamps disagreed on no-room (case file left text painted outside its band, debrief hid it); selectPrimaryControl THROWS and was called unguarded inside render(), the 1.10 stranded-router door; the request control was armed unconditionally so a repeat earned an unearned refusal; a successful pin silently destroyed standing peer feedback (now reported, Alexis's call); error.conclusion-not-ready named the theory board, which lists nothing (now names the case file, AC7's own title made true). Test-side: two tautological expect(x).toBe(x), a pane test that passed with the pane fully armed, and two 'starts no tween' tests asserting nothing (sceneSlice now records tweens). The sweep measured the recognition status against 150px while the renderer paints 96 - a mutation at 129px passes the old bound and fails the new one. 6 guards mutation-proven. 1126 tests / 67 files; e2e 53/7 on an idle machine, identical to baseline. NOTE: three earlier e2e runs showed 8-10 failures purely from review-agent CPU contention (load 46 on 18 cores) - the canvas walks are frame-timed and the config's 'three consecutive runs identical' holds only unloaded. Deferred: the sceneSlice height:18 harness gap (highest value), CASE_FILE_READINESS_ROWS=11 silently dropping a 12th code, and WALK_TO_DEBRIEF_COST_MS re-derived but the walks still ~11-17s against 30s. apparatus.reset and consultation.requested STILL unowned - decide before 2.12.
  2-12-retire-dom-presentation-panels: done  # returned from review 2026-08-07: two code findings fixed; NFR1's manual 10-minute low-end-hardware profile remains deferred and blocks done.
  epic-2-retrospective: optional
  epic-3: done
  3-1-incremental-reusable-case-contract-hardening: done  # reviewed 2026-08-19, 21 patches applied + 5 decisions resolved. Three parallel layers. The story's scope discipline was exemplary (zero Phaser files, zero migrations, no registry layer, fixture sound, test arithmetic exact) and it re-stated the `primaryControls` tuple's guarantees beautifully — MAX_PRIMARY_CONTROLS asserted against real bench geometry. Then it relaxed FIVE more shapes and re-stated nothing for four: `contextualArtifacts` (vs a 2-row case file - a third source is readable, gate-counted and never citable), the three `requirements` counts (minimumSources > authored sources, and minimumSignificantRuns above the reachable configuration space, both unfinishable), `criticalModelInputIds` (enum -> any string: a one-letter typo loads clean and silently collapses the wavelength dimension out of configurationKey), and `activeControlValues` (strict -> record: extra-key rejection gone). All four now enforced. THE COMMENT-VS-GUARANTEE LESSON: three of the four shipped with a comment asserting the check existed; `activeControlValues`' claimed the validation loop rejected an unauthored key, and the loop iterates the DEFINITION so such a key is structurally invisible to it — and the test named for that case passed only because it also dropped a control. Mutation-proved: the new row fails with the guard disabled, the old row stays green. AC5's placeholder rule validated on `\w+`, so `{run-count}`, `{ runCount }`, `{runCount` and `{{runCount}}` (renders `{2}`) each printed themselves into the player's record — now validated on braces. Alexis authorized breaking the scope boundary (decision 1c) to fix the three Young-control-id render paths: two throw inside dispatch->notify (the 1.10 stranded-router mode) and one calls setY(NaN) silently; `findPrimaryControl` is the new fallible seam. Also: the wavelength refusal the player READS still hard-coded 'two' and '550 nm' — the one copy the story set out to de-duplicate that it missed; now interpolated in both locales. The record now states apparatus settings and configurations separately (decision 2c) because Young's rule counts wavelength, so 'distinct apparatus settings' moved without the bench moving — reachable in normal play. `case.json` 1.18.0 with the compatibility clause. 1293 tests / 71 files (from 1270), e2e 59 passed, typecheck clean. BIGGEST FINDING IS THE HARNESS: `tsconfig.json` includes only `src`, so the test suite has NEVER been typechecked — the story diagnosed it, fixed the 11 symptomatic call sites, and left the root cause. `tsconfig.test.json` now exists and `npm run typecheck:tests` is RED with 106 errors across 56 files, deliberately not gated in CI so the pipeline stays green. That count is the metric and closing it is its own story — ASSIGNED to Alexis to schedule. Deferred: per-case experiment-model seam (3.2, the bench refuses every run for a non-Young case), `experiment.wavelengthNm` authored-and-unread, the persisted 450|550|650 unions, and two minimum-mode `550` literals.
  3-2-reviewable-morley-miller-prototype: done  # reviewed 2026-08-19 — three parallel layers, 33 raw findings -> 25 after dedup: 6 decisions (all resolved by Alexis), 21 patches (20 applied, 1 returned), 1 deferred, 3 DISMISSED as false. The two dismissals matter: the hoisted `reduceRecordRun` guard does NOT reject saved records (restore runs through `createAppStateFromCaseRecord` -> `createRunRecord`), and `configurationKey` IS behaviour-preserving for Young (its `significanceRule` carries `criticalModelInputIds: ['wavelengthNm']`). THREE findings said the framework did not yet carry a second case end-to-end, all confirmed against source: (1) a COMPLETED prototype record was discarded on every reload — the completion walk hard-required `modelInputs` then called `calculateYoungFringeSpacing` unconditionally, while the pre-completion walk 90 lines above was already guarded, so the two walks disagreed about whether a second case may finish; (2) the bench ignited for 2.4s and resolved onto a BLANK screen — `paintFringes()`'s only call site sits after the Young-geometry early return, while `paintLight`'s `dark` flag no longer consults `modelInputs`, and the renderer comment claimed the opposite outcome in writing (the comment-vs-guarantee lesson, third story running); (3) `sw.js` CACHE_NAME still v7 with `title` and `modelId` newly REQUIRED, so a cached 1.18/1.19 case.json boots to 'content unavailable'. None was visible to 1334 green tests: the bench tests assert texts(), the e2e header says it asserts no rendered string. Also: rotationDeg 0-180 against a cos(2theta) model made the travel ENDPOINTS one reading (0deg and 180deg both 0.11) and the e2e walk dragged exactly that pair while asserting two DISTINGUISHING runs — now 0deg/90deg (0.11 vs 0.09), with bathTempC added to criticalControlIds so the pair the case's own resetPath teaches stops being refused; 'fringe widths' and the case-file observation row were still canonical English on French surfaces (CaseFilePresenter showed NO settings at all for the prototype — the fourth surface making the same two reads, missed because it is the only one not in the story's file list); rotationDeg 135 at the stable window rendered '-0'; the 1905 artifact declared kind:'transcription' over prose its own reuseStatement says was written for this investigation, and was named 1907 throughout against a real 1905 citation (new 'reconstruction' rendition kind; naming aligned to the verifiable anchor); FR 'de Ecartement des fentes' was a GRAMMAR REGRESSION on shipped Young content (authored `inlineLabel` per control, case.json 1.20.0); `lab.title` was dead in src but still swept and still asserted through a stub whose docstring had gone false; the rounding test compared its output to itself. AC9 clause 2a AMENDED (Alexis) rather than reverting the unrequested model<->control pairing rule. 1339 tests / 74 files (from 1334/74), e2e 61 passed, typecheck clean. 2 new mutation proofs (blank screen, discarded completion), each broken and restored. FINAL ITEM CLOSED in the same review (Alexis's call): the bounded-null claim asserted a steady bath temperature no predicate enforced, and the OBVIOUS FIX WOULD HAVE BEEN WORSE — `evaluateSupportPredicate` reads every recorded run, not the pinned pair, and `experiment.resetPath` tells the player to move the bath and come back, so an all-runs `unvaried-control` would have made the headline conclusion unreachable by following the case's own teaching. Closed properly instead: `AuthoritativeEvidence` gained `selectedRunIds`, both call sites thread `state.theory.selectedRunIds`, and `unvaried-control-pinned` (fail-closed, pinned scope) is authored on the claim; the unscoped variant was removed from the support union rather than left unused. Young untouched. 5 mutation proofs in all across the review, each broken and restored. Final: 1344 tests / 74 files (from 1334), e2e 61 passed, typecheck clean, `typecheck:tests` at 114 — NET ZERO vs the true HEAD baseline, which is 114/59 and NOT the 106/56 the story claimed (measured by stashing; that figure was already stale). AC7 stays PARTIAL by design: the 1887 excerpts' transcription fidelity and the scholarly sign-off need the human reviewer Alexis assigns.
  3-3-source-and-rights-ledger: done  # reviewed 2026-08-19 — three parallel layers, 44 raw findings -> 29 unique: 5 decisions (all resolved by Alexis), 18 patches applied, 2 deferred, 2 dismissed. THE REVIEW'S FIRST FINDING WAS ABOUT ITS OWN CONTEXT: the story asserted twice that project-context.md does not exist, when it existed at the story-creation commit (177 lines) and was expanded to 215 in the baseline commit immediately before dev — so the work was done against unread governing rules and crossed two of them. D1(a): the ?ledger=1 surface is RETIRED. It was a fourth module in src/ui/, which project-context.md holds to exactly three ('Do not add a fourth'), and a fourth visible element outside #game-container where the rule allows three; the spec argued it joined ValidationSessionDisclosure's exemption class, but ADR-007's exemption is a CAPABILITY one (print/export cross a boundary a canvas cannot) and game-architecture.md:441 assigns rights presentation to src/domain/sources/ and LibraryScene. Epic AC1 says 'when a reviewer OPENS its ledger' and names no route, so it is now `npm run audit:ledger` writing docs/source-rights/<case>-ledger.<locale>.md, exit 1 when blocked and 2 on an unknown case id — a gate a release step can use, not just read. The pure projection moved intact to src/domain/sources/ledgerReport.ts; all 13 bilingual assertions already went through it, so only DOM rendering was lost. D3(a): THE GATE FAILED OPEN. A comment stood where two of its five role checks belonged, so either ADR-008 role authored `pending` returned {decision:'clear',blockers:[]} while the sign-off table read Pending — proven by execution by two layers independently, and it failed the story's own Dev Notes §10 test (deleting the comment broke no fixture, because there was no rule). Both now route through roleBlocks; two blocker kinds added (seven, not five) and LedgerBlockerKind is now DERIVED from the list so a kind cannot ship without a sentence. Current blocker sets unchanged. D2(ii): quantique-logo keeps status 'reviewed' on Alexis's confirmation the mark is the project's own, but reviewerState drops to 'pending' — no name, no date, and AssetRights has no fields to carry them, against the story's own README. D4(a): 'de-scoped' removed from the row schemas (a row has no reference to record the decision with; nothing authored it). D5(a): AC11's 59-file cap reduced to the project's error-count metric. Also: R4 extended to de-scoped names/dates and stray references; R1 given its converse on both halves; THREE GUARDS WERE MUTATION-GREEN and are now proven — the AC8 rendition fallback (its own new test never reached the changed line, both fixtures contained an en rendition and asked for 'en'), the cross-file rights comparison (no fixture parsed cleanly while disagreeing), and the no-waiver guarantee, whose single assertion expect(fn.length).toBe(1) is satisfied by the defaulted parameter it forbids. The I18n roster's length assertion was a TAUTOLOGY under a comment claiming it caught exactly that. The replacement-plan sweep executed ZERO assertions for morley-miller. Four FR defects: 'est levé' (masculine singular over two feminine subjects, in a banner no test rendered), 'Relecteur accessibilité' missing its elided preposition, the inflecting 'Validé' shared across four tables, and 'generated derivative' reaching French through a canonical field. Content: Young 1.22.0/1.3.0, prototype 1.2.0/1.2.0, sw.js v10 — a DIFFERENT reason from v9: no field became required, but the story's own manifestsMatch rights comparison makes a mixed-version cache a hard manifest-mismatch. AC1/AC3/AC6/AC11 re-worded where the implementation proved them wrong; 5 spec defects recorded. Gates: typecheck clean, 1403 tests / 77 files (from 1392), e2e 61 passed on three identical idle runs (from 65 — the four retired ledger specs), typecheck:tests 114/60 exactly at the accepted cap. 5 mutation proofs, each broken and restored. Deferred: nothing anywhere gates on the ledger decision (owner Story 7.3), and rightsStatus has no member meaning own-work.
  3-4-scenario-and-proposal-authoring-contract: done  # reviewed 2026-08-19, 32 patches applied, 1 withdrawn, 2 deferred, 6 decisions all taking the recommendation. Three parallel layers, 45 raw findings -> 35. THE STORY'S OWN WORK HELD: scope boundary uncrossed on all eight of Dev Notes SS1, and nine of its seventeen mutation proofs re-run in a clean worktree all went red as recorded. Findings concentrated in two places. (1) A VERSION BUMP TRACED THROUGH THE SERVICE WORKER BUT NOT THE RECORD ALLOWLIST: Task 3 moved the prototype to 1.3.0 and CaseRecordSchema's prototype clause stopped at 1.2.0, so every saved Morley-Miller investigation was refused with incompatible-case-record - the failure the comment three lines above it names from when it happened at 1.1.0. Bumping the version and extending the allowlist are one action, now stated at the site. (2) SIX NEW TESTS WHOSE NAMES DESCRIBED GUARDS THEY DID NOT EXERCISE, four mutation-verified green with the guard deleted: the arrow-key capture release (never focused an instrument, so never took the capture 2.10 fixed), the slider's step affordances (averaged rectangles over the whole bench, 8.5 against a floor of 2, so deleting the slider's pair left 1497 green), the reduced-motion check (insensitive to the flag), the affordance vocabulary (a tautology that never imported the schema), the router-reuse test (never moved the phase), and an expect(checked).toBeGreaterThanOrEqual(0) whose comment deferred its floor to the tautology. All six now fail under the mutation they are named for. Also: THE DIAL ALIASED ITS ENDS so control.max was unreachable by drag on any dial while the keyboard could step onto it - an ADR-012 violation the drag/step sweep had been shaped around with a slice(0,-2) carve-out; travel now seamed at stepCount/(stepCount+1) of the circle and the carve-out is gone. The slider slammed to min on a non-finite pointer where both rotary affordances hold. The dial's index mark was painted entirely outside its own hit area. A sixth cast refinement refuses a board whose proposers the scene does not stage - which has a design consequence worth knowing: a cast can only narrow against colleagues who author no proposal on that board, so neither shipped case can narrow at all, and the worked example gained a non-proposing archivist to demonstrate the field honestly. The guide promised refusal messages loadCaseDefinition discarded; issues are now logged with their paths. ONE GUARD COULD NOT BE MADE FALSIFIABLE AND IS RECORDED AS SUCH: instrumentBand's dial arm ternary is between two constants that are both 54 by design, so hard-coding it to the knob's is invisible to any value-based assertion - the equality is now pinned so the test goes live if they ever diverge. Corrected in the record: 28 quoted refusal messages was 35, AC9's "59 files" was 60, and a comment crediting MorleyMillerPrototype.test.ts with an assertion it does not contain. Four new mutation proofs (18-21), including the snap-before-dispatch one Dev Notes SS13 item 6 asked for and the original table omitted. Gates: typecheck clean, 1503 tests / 79 files (from 1497), build ok, e2e 62 passed on three runs (from 61 - the new one is the slider walk, which had NO real-input coverage anywhere before), typecheck:tests held at 114/60 by diffing against a stashed baseline. Deferred: the harness discards zone constructor geometry and draw-command coordinates (joins the text-height gap as top harness item), and configurationKey counting a cyclic control's aliased ends as two configurations - PREDATES 3.4, owner unassigned, and the dial made it easier to hit rather than causing it.
  epic-3-retrospective: optional
  epic-4: in-progress  # first story of epic 4 contexted 2026-08-20
  4-1-morley-miller-historical-case-record: done  # code review complete 2026-08-20, 22 patches applied. Dev: 1907 final report re-anchored (full transcription, Science XXV p.525); 1887 excerpts VERIFIED against the cited issue's facsimile -- 3 divergences fixed, pages [333,334] and [341]; bounded-null (1.53 wavelengths / one eightieth) reaches curated record + debrief; cycles 6->4 with a Morley-Miller refinement; new pure campaignOrder.ts, boot default FLIPPED to the campaign entry (validation route held on Young). Review found and fixed: the French typography sweep's cross-case extension was a no-op (longestFrench picked Young for all 6 constants) and the debrief prose was swept by nothing -- SAMPLE_PARAMS now holds one parameter set per case and the debrief/consultation prose is measured; sw.js gains an install-time precache (v12->v13) closing an offline dead end reachable by completing case 1; the 1.4.0 record clause now accepts prior versions for records that cannot name the retired artifact, so in-context progress is no longer discarded and overwritten; the completion probe no longer routes a player past a case on a record the app will refuse; gotoCase's implicit-Young default removed at all 17 sites; a stale 1905-era consultation line and the 1887 elision marks corrected; 4 residual owners assigned (4.3 / Epic 5). case.json 1.4.0 (unreleased, so unbumped by the patches), ledger still BLOCKED (reviewer + educator sheet unassigned -- Alexis). Gates: typecheck clean, 1527 unit/80 files, build + build:subpath ok, 67/67 e2e, typecheck:tests 106/60 unchanged. 7 further mutation proofs; AC3/AC4 verified by screenshot at 1280x720 EN+FR (debrief not reachable -- recorded for 4.3).
  4-2-thermal-drift-investigation-tutorial: ready-for-dev  # story contexted 2026-08-20
  4-3-morley-miller-bounded-conclusion-and-debrief: backlog
  epic-4-retrospective: optional
  epic-5: backlog
  5-1-hafele-keating-predictions-and-source-record: backlog
  5-2-clock-model-and-error-budget-investigation: backlog
  5-3-relativity-conclusion-and-debrief: backlog
  epic-5-retrospective: optional
  epic-6: backlog
  6-1-hensen-case-record-and-experimental-safeguards: backlog
  6-2-chsh-reliability-and-finite-trial-investigation: backlog
  6-3-entanglement-conclusion-and-critique: backlog
  epic-6-retrospective: optional
  epic-7: backlog
  7-1-educator-case-materials: backlog
  7-2-cross-browser-release-verification: descoped  # pivot: a11y dropped from MVP (retitled from accessibility-and-cross-browser); revisit post-MVP
  7-3-static-release-and-source-rights-sign-off: backlog
  epic-7-retrospective: optional
