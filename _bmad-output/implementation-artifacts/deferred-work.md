# Deferred Work

## Epic coverage-map story references

The current `_bmad-output/planning-artifacts/epics.md` FR coverage map still contains pre-existing references to nonexistent Story 1.10 and a likely stale reference from FR11 to Story 1.9. Reconcile FR11, FR17, and FR28 with the actual Epic 1 story numbering in a dedicated traceability pass.

## Deferred from: code review of 1-7-consultations-peer-review-and-revision-history (2026-08-04)

- Provide a player-facing non-destructive reset surface — only a pure phase-reset helper exists; this is pre-existing and not altered by Story 1.7, though AC 8 expects reset access.

## Deferred from: review of improve-reference-mechanism (2026-08-05)

- **Stale e2e notebook button.** ~6 e2e specs (`curated-record` snapshots test, `accessibility`, `theory-board`, `offline-reload`, `progress-portability`, `inquiry-recognition`) click a button named `Record prepared observation` that no longer exists in `src/` — the notebook record button now needs a `prepareRun` arg `main.ts` never passes (and is labelled `Record fixture observation`). These fail on baseline `090356b`. Reconcile the notebook-recording e2e flows with the current Phaser-laboratory "Run experiment" recording path.
- **Run-experiment disabled-state e2e mismatch.** `young-experiment.spec.ts:19` asserts `aria-disabled='true'` then clicks "Run experiment", but the button is hard-`disabled`, so Playwright cannot click it. Pre-existing on baseline. Decide whether the control should be soft-disabled (`aria-disabled` + click-to-explain) or update the test.

## Deferred from: code review of 1-10-scene-router-and-adventure-flow (2026-08-05)

- **~~`LectureBookScene` couples directly to `LaboratoryScene` instead of routing through the store.~~ CLOSED by Story 2.8 (2026-08-07).** The scene is deleted. The book is now a `ReferenceBookPresenter` owned by each scene that can host one (`LibraryScene`, `LaboratoryScene`), so the scene that draws it is the scene that suppresses itself — no reach-in, and no un-routed scene, so "the active scene" is single-valued again. The `scroll` listener moved to `adapters/phaser/canvasBounds.ts` and is registered and disposed by every routed scene's own lifecycle; the renderer is released in each presenter's `destroy()`. The `isOverlayVisible` parameter was **removed** from the five scenes that took it rather than defaulted, for the reason the 2.7 review gave. Original finding follows. `src/game/main.ts:50-53`. The persistent book overlay is auto-started, never routed, and suppresses apparatus input via `(visible) => laboratoryScene.setApparatusInputEnabled(!visible)` — direct scene→scene reach-in, outside the store pattern the architecture's Communication rules prescribe. **Owned by Story 2.1**, which folds the reading experience into `LibraryScene` and retires the coupling; deferring avoids building a store-routed overlay slice that 2.1 would discard. Carry forward: the game always has ≥2 active scenes (so "the active scene" is no longer single-valued), and because nothing ever stops this scene its `scroll` listener and `LectureBookRenderer` are never released. Also note Task 3's "preserve the existing `onLectureBookReady` wiring for `LaboratoryScene`" was marked complete but the constructor parameter was in fact removed.
- **Production offline-readiness race before the worker caches `asset-manifest.json`.** `tests/e2e/offline-reload.spec.ts:73` gained a wait for the warm-up boot to finish before cutting the network. That fix is legitimate and masks nothing — the race reproduces 8 / 8 at baseline `c5eba0f` under `--workers=1`, i.e. fully independent of the scene-router work, contrary to the story's Debug Log claim that routing shifted boot timing. What remains untracked is the production window itself: a real user who reloads and loses connectivity before the fetch-through worker has cached `case.json` and `asset-manifest.json` gets `content-unavailable` and a blank boot rather than a degraded one. Decide whether the worker should pre-cache content atomically (cache-then-activate) instead of caching as it fetches.
- **`#game-container` is `aria-hidden="true"` while becoming the authoritative surface.** `index.html:36`. Story 1.10 makes the routed Phaser canvas the surface that mirrors the authoritative phase, and later stories (2.1 Library, 1.11 Colleagues, 1.6-rework/2.3 Theory Board, 1.12 dialogue) move real content into it. Any scene-only content is unreachable for assistive tech, and outside `experiment` there is no DOM mirror. A11y projection through the Phaser surface was descoped for this story; it needs an owner before the DOM panels are retired.

## Deferred from: code review of 1-1b-internationalization-foundation-english-french (2026-08-06)

- **Save-compat allowlist leaves 1.0.0/1.1.0 records unreachable, and nothing guards the next bump.** `src/schemas/CaseRecordSchema.ts:143-145`. The `definition.version === '1.2.0' && ['1.0.0','1.1.0']` clause can never fire — the shipped definition is 1.6.0 — so it reads as protection it no longer provides. Pre-existing: at baseline `128f1bf` the definition was already 1.4.0, so that clause was dead then too, and this story's `1.6.0 && ['1.2.0'..'1.5.0']` addition strictly widened compatibility. Carry-forward risk: the allowlist is only sound because the canonical English `feedback` / `revisionPath` strings are byte-identical across those versions — `validateCaseRecordForDefinition` recomputes and `JSON.stringify`-compares them. Nothing enforces that for the next bump; the author who reworks an English feedback sentence and extends the array ships a silent load failure. Open Question 5 already asks whether to replace the ad-hoc allowlist with a declared compatibility range — do that before Epic 3 adds case versions.
- **`LOCALES` is presented as the single source of truth but controls nothing that matters.** `src/core/i18n/Locale.ts:5`, `src/schemas/CaseDefinitionSchema.ts:30,85,92`. Adding a third locale makes `LOCALES.length === 3` while `renditions` stays a fixed 2-tuple whose `Set` size can never exceed 2, so every case fails to parse with "A readable source must provide exactly one rendition per shipped locale" and the app boots into "content unavailable" with no clue the tuple arity is the cause. `LocalizedTextSchema` / `LocalizedTextListSchema` also hardcode `en`/`fr` under `.strict()` and would reject the new key outright. Deferred because EN+FR is hard-scoped (NFR19, ADR-010, GDD Out-of-Scope) — but if a third locale is ever considered, the real arity lives in a tuple literal, two hardcoded object schemas, and the `LocalizedText` type, not in `LOCALES`.
- **`locale` is threaded as an optional parameter with a silent `DEFAULT_LOCALE` default.** `src/core/store/AppState.ts:147,165` (`createInitialAppState`, `createAppStateFromCaseRecord`), `src/adapters/phaser/renderers/LectureBookRenderer.ts:72` (`getLocale: () => Locale = () => 'en'`). Each default converts "a call site forgot the locale" from a `tsc` error into a French player silently seeing English, in a design whose whole premise is that the locale is never captured or omitted. Four call sites must remember it today. The defaults exist to avoid touching test fixtures, and `tests/unit/LocaleStore.test.ts` now encodes the escape hatch as intended behaviour. Make the parameters required when the fixtures are next touched.
- **~~The book control width exists as three unlinked magic numbers.~~ CLOSED by Story 2.8 (2026-08-07, AC7).** `LectureBookRenderer` exports `BOOK_CONTROL_WIDTH`, `BOOK_CONTROL_HEIGHT`, `BOOK_CONTROL_PADDING`, `BOOK_CONTROL_FONT_SIZE`, `BOOK_CONTROL_MIN_FONT_SIZE` and `bookControlLabelWrap()`; the draw call, the shrink bound, the hit test, and `french-typography.spec.ts` all read them. The control row's three x positions — each previously a literal written twice, once in `drawSpread` and once in `activateControl` — were named too. The shrink loop now starts one step below the authored size. **The rationale originally recorded here was wrong and is corrected by the 2.8 review:** the old loop evaluated its condition *before* the first body execution, so a label that already fitted never entered it at all — the wasted iteration only ever happened to labels that were overflowing anyway. The new loop is behaviourally identical, which is fine; there was no per-redraw cost to remove. Original finding follows. `src/adapters/phaser/renderers/LectureBookRenderer.ts:272` draws the button at a literal `150`, `:281` shrinks against `CONTROL_WIDTH - 16`, and `tests/e2e/french-typography.spec.ts:117` asserts against a literal `134`. Nothing ties them together, so changing the button width silently desynchronises the shrink bound and leaves the e2e test passing against a stale number. Same function: the shrink loop's first iteration is a no-op (it measures at 15px then calls `setFontSize(15)`), burning one measure/reflow per control on every spread redraw.

## Deferred from: code review of 2-4-young-learning-and-educator-validation-gate (2026-08-06)

- **`?mode=validation` is matched by exact case-sensitive equality, and the guard fails open into persistence.** `src/main.ts:26`. `?mode=Validation`, `?mode=VALIDATION`, `?mode=validation%20`, or `#mode=validation` all yield `validationMode === false`, so `CaseRecordRepository` is constructed, `repository.load` runs, and `mountCaseProgressPanel` mounts with its autosaving subscription — a facilitator typo runs a moderated session against the learner's real IndexedDB record with no disclosure rendered and no visible signal that isolation was never engaged. The unsafe direction is the default. Pre-existing; Story 2.4's Task 3 was explicitly verify-only, and only the exact literal is ever exercised in tests. Decide whether to normalise (`trim`/`toLowerCase`) and whether an unrecognised `mode` value should refuse to boot rather than silently falling through to the learner route.
- **`SceneRouter` writes to the console on the validation route.** `src/adapters/phaser/SceneRouter.ts:92` — `console.error('The scene router could not activate the scene for the current phase.', error)` is not gated on `import.meta.env.DEV`, and the router is constructed unconditionally in both modes (`src/main.ts:120`, outside the `if (repository)` gate). Story 2.4 checked off "Validation mode must still write nothing to … console" on an audit that named only the two i18n warns (both correctly DEV-gated) and missed this one. Pre-existing from Story 1.10, carries no learner text, and is a defensive error path only — but the subtask is marked verified and is not strictly true. Either DEV-gate it or amend the claim.
- **Six firefox/webkit baseline e2e failures are untracked and unowned.** Surfaced while Story 2.4 established the cross-browser baseline on `87f0ab5`, recorded in `docs/validation/young-technical-evidence.md`, and carried here at that story's own request. `accessible-control.spec.ts:56`, `dialogue-advance.spec.ts:68`, `dialogue-advance.spec.ts:98`, and `scene-router.spec.ts:31` fail in **both** firefox and webkit; `offline-reload.spec.ts:17` (French offline reload) and `offline-reload.spec.ts:137` (cached validation route) fail in **webkit only** — the latter with `page.reload: WebKit encountered an internal error` at the offline step. These need an owner before cross-browser release verification (Story 7.3) can mean anything, and before the blocking "Automated technical evidence" gate can ever be recorded Pass.
- **The whole-app boot guard requires thirteen retired-panel roots, and failing it is silent.** `src/main.ts:27-45`. A single 15-clause `if (!… || …) return;` runs before `createBootShell`, so removing any of `#curated-record`, `#case-context-prediction`, `#apparatus-controls`, `#measurement-notebook`, `#theory-board`, `#consultation-panel`, `#conclusion-review`, `#decision-history`, `#inquiry-recognition`, `#historical-debrief`, `#case-progress`, or `#print-record` from `index.html` leaves the page on its static markup with no click listener, an empty `#boot-status`, and no validation disclosure — nothing logged, nothing shown. `#case-progress` and `#print-record` are required even in validation mode, where they are never used. This directly obstructs the documented pivot step of deleting the retired panels. Related: `expect(entryButton).toHaveText(en['boot.enter'])` in `tests/e2e/validation-route.spec.ts` passes even when JS never hydrated, because the label is static in `index.html`.
- **Retiring `CaseProgressPanel` will silently hollow out the validation isolation spec.** `tests/e2e/validation-route.spec.ts` asserts `getByRole('region', { name: 'Save, export, import, and print' })).toHaveCount(0)` on the validation route (and `tests/e2e/offline-reload.spec.ts:148` does the same) — both become trivially true on **every** route once the panel is deleted, so they stop verifying isolation without failing. The `print.ariaLabel` article assertion survives (ADR-007 retains the print view) but proves only the print view's absence. Separately, `seedSavedProgressOnNormalRoute` reaches that panel's "Save progress" button as the only save affordance in the suite, so the same deletion leaves the byte-for-byte isolation proof with no way to establish its precondition. A non-panel seeding path is needed before the panel goes.
- **The e2e IndexedDB probe duplicates persistence constants that source owns.** `tests/e2e/validation-route.spec.ts` hardcodes `'quantique-progress'`, version `1`, and store `'case-records'`, all of which currently match `src/adapters/persistence/IndexedDbRepository.ts:15-19` — but the project testing rule is "never assert a magic number that a test shares with source unless both read one exported constant". A version bump turns the probe into a `VersionError` in every run; a store rename turns it into a silently created empty store, because the probe's own `onupgradeneeded` creates the store. Export the three from the adapter and read them in the test.
- **Playwright's browser locale is never pinned for the English e2e cases.** `playwright.config.ts` sets only `baseURL` and `trace` under `use`, and the three projects spread `devices[...]` with no `locale`. The French cases pin their input with `test.use({ locale: 'fr-FR' })`, but "English" is left as an ambient host property, so every `en[...]` assertion added by Story 2.4 depends on the host's system language. Not reproducible on the current machine (all three projects reported `navigator.language === 'en-US'`), and `src/core/i18n/Locale.ts` asserts in prose that "E2E specs stay deterministic because Playwright defaults to an English context" — a claim enforced nowhere. Pin `locale: 'en-US'` in the config so the default is a decision rather than an inheritance.
- **`docs/i18n-authoring.md` contradicts the captured-`locale` pattern the code now uses.** The doc states "The active locale always comes from the store: `selectLocale(state)`. Never from a local field or a constructor argument captured once", while `BootShell` and (as of Story 2.4) `ValidationSessionDisclosure` both take a captured `locale` parameter — deliberately, because a required parameter makes a forgotten call site a `tsc` error. The code is right and the doc is stale; reconcile it before it is cited against a future story.

## Deferred from: code review of 2-5-rival-lab-critique-and-revision (2026-08-06)

- **~~A fourth unlinked copy of the book-control coordinate.~~ PARTLY CLOSED by Story 2.7 (2026-08-07).** `LectureBookRenderer` now exports `bookCloseControlCentre()`, and both `rival-lab.spec.ts` and the new `canvas-transitions.spec.ts` derive the coordinate from it — closed because 2.7 needed a *second* copy of the literal and duplicating it would have been the violation. **Fully closed by Story 2.8's review (2026-08-07); it was recorded as closed by the story itself, which was not accurate.** The design dimensions live in `src/adapters/phaser/designSurface.ts` — a module with no imports at all, so both the Phaser game config and the specs read the same two constants. As shipped, three specs the story edited still restated them: `french-typography.spec.ts` declared a *fresh* `1024`/`768` pair in the same commit that created `designSurface.ts`, and `rival-lab.spec.ts` and `dialogue-advance.spec.ts` each kept a private design-space mapping. All three now read the shared module, `artifactAt` and `canvas` live once in `tests/e2e/canvasHelpers.ts` rather than per spec, and the room's six private font sizes are exported and read rather than copied into the typography sweep. `accessible-control.spec.ts` and `scene-router.spec.ts` still restate the pair; neither was touched by this story, so they are the remaining tail of this item. The width triple is closed in its own item above. The book's open, page-turn, and close durations are exported for the same reason: a spec has to wait each of them out, and a literal would silently stop covering the window the day the tween changed. Original finding follows. ~~`tests/e2e/rival-lab.spec.ts:28` declares `const BOOK_CLOSE = { x: 512, y: 678 }`, restating `LectureBookRenderer.ts` `CONTROL_Y = 678`~~ — in a spec whose own header claims "Both click targets are **derived** from the renderers' exported geometry rather than restated as literals — the rule the 1.12 review set". The submit and revise targets genuinely are derived; this one is not. If the book's close control moves, the walk-in helper silently leaves the reference book open, every subsequent canvas click is legitimately suppressed, and the spec fails with a routing error pointing nowhere near the cause. The `768`/`1024` design dimensions are likewise restated three times while the renderer reads `this.scene.scale.height`. Same class as the already-tracked `LectureBookRenderer` width triple; fold both into one exported-geometry pass.
- **The French e2e case asserts nothing locale-dependent.** `tests/e2e/rival-lab.spec.ts` — the walk-in helper hardcodes English DOM accessible names (`Continue to prediction`, `Tentative prediction`, `Record a prediction`, `Continue to experimentation`, `Run experiment`, `Continue investigation to synthesis`), taking only `boot.title` and `curatedRecord.inspect` from the bundle, and the `fr-FR` case then asserts only `data-active-scene` values, which are locale-invariant. It would pass with every French rival-lab string deleted. Its own comment concedes canvas text is unassertable in Playwright, which is true and pre-existing — but the conclusion drawn ("the French bundle demonstrably live") is stronger than the assertions support, at the cost of a second full browser walk. Either derive the DOM names from the bundle so the walk itself proves the locale, or drop the claim.
- **~~`french-typography.spec.ts` cannot detect a fixed-height control label wrapping to two lines.~~ CLOSED in review of 2.5 (whole-string test added) and extended by Story 2.7 to every fixed-height control there is — the six `advance.*` labels each against their own host's bound. Original finding follows.** The `WRAPPED_SURFACES` sweep splits each string on whitespace and measures per token, so `theoryBoard.submit` (FR "Soumettre cette conclusion", 212px wrap inside a 34px-high rectangle) and `rivalLab.revise` are only checked token-by-token. The suite has a dedicated whole-string test for book controls (`fits every French book control inside its fixed button width`) and none for these two. Exactly the pre-existing gap `dialogue.advance` already sits in; the two new controls inherit it rather than closing it. Extend the whole-string control test to cover every fixed-height labelled control.
- **Two vacuous unit tests, and the documented authored-order tie-break is untested.** `tests/unit/RivalLabRules.test.ts` — `takes the first authored critique for a proposal, in authored order` computes its expectation with the identical `.find` predicate the implementation uses, so it asserts `find(...) === find(...)`; it could only distinguish "first" from "last" against content carrying two critiques for one proposal. `is deterministic across repeated calls` calls a dependency-free pure function twice over a frozen module-level fixture and cannot fail short of the JS engine breaking. The schema permits multiple critiques per proposal (`z.array(...).min(1)`, coverage checked only for at-least-one) but the authored case has exactly one each, so the tie-break rule `selectRivalLabCritique` documents has no coverage. Add a two-critique fixture.
- **`critiqueHistory` grows without bound or dedupe on repeated submissions of the same proposal.** `src/core/store/AppState.ts:724` appends unconditionally, so submit → revise → submit the same indefensible proposal writes a near-identical entry each cycle (differing only in timestamp), and the log is autosaved and snapshotted into `completion`. `validCritiqueHistory` accepts arbitrarily many duplicates by design, and the only growth test uses two *different* proposals, so the repeat case is unmeasured. Benign at play-session scale; revisit if the record ever gains a display surface or a size budget.
- **`rivalLab.name` renders the English honorific under FR.** `public/cases/young-interference/case.json` — `"name": "Mr. Arthur Bell"` is correctly a plain untranslated string (Task 1, and project-context's "Proper nouns stay plain strings"), but `formatAttribution` composes it with the localized role, so a French player reads "Mr. Arthur Bell — Laboratoire rival". French would normally use "M.". A copy decision, not a code defect: either accept the English honorific as part of the proper noun, or split the honorific out of the name so it can be localized while the name stays fixed.
- **~~`critiqueHistory` is persisted but displayed on no surface.~~ CLOSED by Story 2.11 (2026-08-07).** The debrief renders it, paged one challenge at a time at full room width, through `selectLocalizedCritiqueHistory` — which reads **`completion.critiqueHistory`** rather than the live field, so a replay cannot empty it and a re-completion cannot refill it with the second pass's challenges. It is framed as inquiry rather than failure in the heading itself ("Where your claim was tested"), which is AC3's requirement. Original finding follows. ~~AC2 says "the decision record retains the rejected choice and the critique". It is durable in IndexedDB and in the exported JSON, but `grep -rn critiqueHistory src/` outside `core/store` and `schemas` returns nothing — not `CaseRecordPrintView`, not the debrief, not `DecisionHistoryPanel`. No task asked for a display, and the review decision (2026-08-06) was that AC2 is satisfied by the durable fact. Deferred rather than built here because `DebriefScene` is still a `PhasePlaceholderScene`, so a reader-facing surface belongs with the debrief/print pass — building it now would expand 2.5 well past its scope. Owner: whichever story builds the debrief surface.~~

## Deferred from: code review of 2-6-significant-measure-gate-and-colleague-hints (2026-08-06)

- **The retired DOM theory-board panel answers the new significant-measure gate with a raw untranslated English string and no hint.** `src/ui/theory/TheoryBoard.ts:100-105` sets `statusMessage = transition.error.message` verbatim, so a French session at phase `experiment` with one run recorded activates "Continue investigation to synthesis" — still rendered, still in the accessibility tree — and reads the reducer's hard-coded English `'Record a second observation that differs from the first before continuing to synthesis.'` Not `error.significant-measures-required`, which Story 2.6 added to both locales and which only `ApparatusRenderer` consumes (and which it explicitly sets to `undefined` for this code, `:441-442`). Two surfaces now give two different answers to the same refusal, one of them in the wrong language, against the platform rule "never expose a raw error to the player". Deferred because Story 2.6's Dev Notes forbid touching the panel — the pivot retires it and Task 7 exists precisely so the canvas carries the affordance. Owner: whichever story deletes the retired `src/ui/*` panels. Until then the gate's localized message has exactly one consumer and the DOM route has none.
- **The advance control is suppressed below 768px, leaving the retired DOM panel as the only route to synthesis on a narrow viewport.** `src/adapters/phaser/renderers/ApparatusRenderer.ts:521-528` — `updatePhoneReadOnlyMode` gates the new control on `this.inputEnabled && !matchMedia('(max-width: 767px)').matches`. Correct and deliberate for `setInputEnabled` (it stops a reference-book page click falling through and moving the player out of the laboratory, the 1.12 defect), and mobile is not a target platform. But the consequence is that on a narrow viewport the only remaining dispatcher of `nextPhase: 'synthesis'` is `src/ui/theory/TheoryBoard.ts:102`, whose unreachability is the stated reason Task 7 exists at all. Pre-existing phone read-only accommodation; revisit if narrow viewports ever become a supported target, or when the DOM panels are deleted — whichever comes first.
- **~~`transientError` is cleared inside the render that draws it, so any subsequent render wipes a non-gate refusal message.~~ CLOSED by Story 2.7 (2026-08-07).** Both renderers now hold transient messages in `src/adapters/phaser/renderers/transientMessage.ts`, which anchors a message to the `AppState` object it was set against and clears it only when a *different* object is rendered — `createStore` replaces `state` with a new frozen object exactly on a successful dispatch, so object identity is precisely "a real state change happened". Fixed in both renderers in one pass, as this item asked, rather than in the laboratory alone. `tests/unit/TransientMessage.test.ts` asserts the lifetime on the helper. Original finding follows. ~~`src/adapters/phaser/renderers/ApparatusRenderer.ts:469-470`~~ reads `transientError` into `lineText` then immediately sets it to `undefined`, while `advanceRefused` is sticky and clears only when the selector stops returning a hint. So a store-busy refusal (`createStore` short-circuits every dispatch during an exclusive progress operation — a click during an export) paints once and is erased by the next `render(state)`: a control nudge, a recorded run, a locale change, or the export's own completion notify. The player is left with a control that refused for a reason they can no longer read — which is the state `advanceToSynthesis()`'s own comment says swallowing the error would produce. Deferred rather than patched because it mirrors the established `ColleagueRenderer` transient-slot pattern; fixing it here alone would make the two renderers inconsistent. Fold both into one pass that gives transient messages an explicit lifetime.

## Deferred from: development of 2-7-in-scene-phase-transitions (2026-08-07)

- **Two intents in the `review → debrief` chain have no owner in the 2.7–2.12 plan.** `theory.supportRunSelected` / `theory.supportSourceSelected` (`src/ui/theory/TheoryBoard.ts`) and `peerReview.requested` / `revision.saved` (`src/ui/review/ConclusionReviewPanel.ts`) are dispatchable only from retired DOM panels, and no story in the correction claims them: 2.8 takes the library and `source.inspected`, 2.10 takes the apparatus and the notebook, 2.11 takes the debrief surface, and 2.12 deletes the panels. **Story 2.12's completion check — "every player intent is dispatchable from the canvas" — cannot pass while these four stand**, and 2.12 is the story that deletes their only dispatchers, so the gap turns into an unreachable `review → debrief` the moment it lands. Raised as Open Question 1 in the 2.7 story file and unanswered at development time. Decide whether they belong to 2.11 or to a new 2.13, **before** 2.12 is scheduled. **CLOSED by Story 2.11 (2026-08-07):** all six are dispatched from the case-file overlay on the theory board, through `PhaserStoreAdapter.selectSupportRun` / `unselectSupportRun` / `selectSupportSource` / `unselectSupportSource` / `requestPeerReview` / `saveRevision`. `canvas-transitions.spec.ts`'s gating-intent table is empty and its walk completes the Young case with canvas clicks only.
- **The advance controls outside the laboratory are *not* suppressed below 768px, so the narrow-viewport rule is now inconsistent across hosts.** `ApparatusRenderer.updatePhoneReadOnlyMode` suppresses the laboratory's control under `(max-width: 767px)` (tracked above); the five controls Story 2.7 added to `ColleagueRenderer` and the routing shell deliberately do not (the shell is gone as of 2.11; its control lives on `DebriefScene` and still does not suppress), because the story's Dev Notes assign the sub-768px decision to Story 2.12 and instruct it to preserve today's behaviour. The result is that on a narrow viewport five of six transitions are canvas-reachable and the sixth is not. Not a regression — the alternative was extending a suppression 2.12 is meant to re-decide — but it makes the inconsistency visible and it should be resolved in one direction when 2.12 owns it.
- **~~`error.conclusion-not-ready` points the player at a surface that is being deleted.~~ CLOSED by Story 2.11 (2026-08-07).** The canvas theory board now shows what is missing, in the case file, through `selectLocalizedConclusionReadiness` — localized by requirement `code` against the existing `conclusion.missing.*` keys, never from the dev-facing `missing[].message`. It reports the player's own record and carries no opinion about which conclusion the evidence supports (ADR-006). Original finding follows. ~~ The FR/EN copy ("The theory board shows what is missing" / "Le tableau de théorie indique ce qui manque") reads naturally as the canvas theory board, but the canvas board shows nothing of the kind — only the retired `src/ui/theory/TheoryBoard.ts` panel enumerates the missing requirements. Observed while manually verifying Story 2.7's refusal registers: the message is localized and non-raw, so it satisfies AC4, but it directs the player to information that does not exist on the surface they are standing on and will not exist anywhere once 2.12 lands. Either the canvas board gains a readiness surface or the copy stops promising one. Owner: the story that closes the `synthesis → review` gating intents (see the first item above).~~
- **~~`PhasePlaceholderScene` now carries player-facing behaviour, which raises the cost of leaving it in place.~~ CLOSED by Story 2.11 (2026-08-07).** `src/adapters/phaser/scenes/PhasePlaceholderScene.ts` and `phasePlaceholderGeometry.ts` are deleted; `grep -rn "PhasePlaceholderScene\|phasePlaceholderGeometry" src tests` returns nothing but historical prose. `DebriefScene` was the last subclass and is a real scene now, reproducing the shell's advance affordance, transient refusal and canvas-bounds refresh verbatim while losing the marker. The rule the item rests on survives the class — see `project-context.md` §Engine, which was rewritten to describe the practice rather than the type.

## Deferred from: code review of 2-7-in-scene-phase-transitions (2026-08-07)

- **`invalid-completion-timestamp` tells a player who typed nothing to "Provide a valid UTC completion timestamp."** `src/core/store/AppState.ts` — two distinct causes share one code and one message: a malformed stamp (`:782`, impossible from the adapter, which stamps `new Date().toISOString()`) and a stamp *earlier than the saved reviewed revision* (`:791-793`). Only the second is reachable, and its copy describes the first. Reachable in normal play via progress import: `replaceWithValidatedRecord` restores `decisionHistory` verbatim from a record exported on another device, so a local clock behind the restored revision's timestamp makes "Close the case" refuse on every click until the wall clock catches up — with copy that reads as a validation error for a field the player has never seen. Also reachable from a backwards NTP or manual clock correction between `revision.saved` and the advance click. The codebase already recognised this class and solved it once: `reduceTheoryConclusionSubmit` (`:730-736`) splits the ordering failure into its own code with device-clock copy (`en.ts:267-269`, "Check the device clock, then submit again"). `case.debriefCompleted` did not get the same treatment, and Story 2.7 is the story that put it behind a canvas control. Deferred rather than patched because a new error code is a reducer change, which 2.7's §Scope boundary forbids. Owner: whichever story next touches the debrief transition (2.11).
- **The outgoing scene repaints with the incoming phase's label for roughly one frame on every transition.** On a successful advance, `notify` iterates the store's listeners and the *outgoing* scene's own subscription still runs, resolving its label from the **new** phase (`ApparatusRenderer.ts:496`, `ColleagueRenderer.ts:319`, `PhasePlaceholderScene.ts:102`); the router's `scenes.stop/start` is then queued by Phaser's `SceneManager` because it is called from inside a pointer handler, so the frame renders with the outgoing scene still active and freshly repainted. Nothing gates the render on the scene still matching the phase. Story 2.6's `t('lab.advance')` was a constant and could not show this; making the label phase-derived introduced it in all five hosts. ~16 ms of "To your reviewers" in the laboratory column, or `Library (placeholder)` under "To the bench". Not verified against a running build — the queueing behaviour is inferred from `SceneManager.isProcessing`, and it may be swallowed by the frame boundary. Confirm before fixing; if real, the guard is one `selectCasePhase` comparison in each host's subscriber.
- **Nothing enforces the type-only Phaser boundary that both geometry modules now depend on.** `apparatusGeometry.ts` and `phasePlaceholderGeometry.ts` exist, by their own headers, so Node-hosted Vitest and Playwright specs can import geometry without Phaser touching `window` at import time. Both now import value exports from `src/adapters/phaser/ui/AdvanceControl.ts`, which is a class that calls `this.scene.add.rectangle(...)`. It is safe today — `AdvanceControl` imports Phaser as `import type { Scene }` only, and its docstring commits to that in prose — but the same docstring anticipates the change that breaks it: "a host that ever did reposition or resize this control would have to write `input.hitArea.width/height` directly", which needs `Phaser.Geom.Rectangle` as a value. The day that happens, every spec importing either geometry module fails at *import*, not at assertion — the exact failure mode both files were created to prevent. A lint rule pinning `phaser` to type-only imports in `ui/AdvanceControl.ts` would make the boundary a build error instead of a convention.
- **The `progress-operation-active` message has no upper bound, because its resolving condition produces no state change.** Deferred by review decision 2026-08-07: *the identity rule stays exact and un-special-cased, and the message self-heals on the player's next successful dispatch.* AC5's rule — clear a transient message iff `state !== anchor` — is exactly as specified and is correct for every other refusal: `missing-prediction`, `missing-contextual-sources`, `conclusion-not-ready` and `reviewed-revision-required` are each resolved by an action that mints a new frozen state, which clears the slot. `progress-operation-active` is the one code whose resolution is a `finally` releasing the lock in `src/ui/persistence/CaseProgressPanel.ts:92-94` — no dispatch, no `replaceWithValidatedRecord`, no new state object. So a "Please wait for the progress operation to finish." set on either board (`ColleagueRenderer.ts:409-414`) occupies the *guide* slot (`:311-313`) until the player's next **successful** dispatch; advancing the dialogue does not clear it, because `onAdvance → relayoutCards()` dispatches nothing by design. Not permanent, but in the interim the board's only instruction line is a stale "please wait" for an operation that has finished. The pre-fix clear-inside-render bounded this at one frame; the fix removed the bound without adding a condition-cleared trigger. The two fixes considered and not taken: clearing the slot from `acquireExclusiveOperation`'s release callback, or giving this one code a bounded lifetime. Revisit if a progress operation ever becomes long enough, or failure-prone enough, that a player is likely to read the stale line and act on it.

## Deferred from: code review of 2-8-library-reading-room-and-reference-book (2026-08-07)

- **Phone read-only mode also suppresses the new bench reference shelf, so re-reading is unreachable on a narrow viewport.** `src/adapters/phaser/renderers/ApparatusRenderer.ts:709-721` — `updatePhoneReadOnlyMode` computes one `enabled` flag (`this.inputEnabled && !matchMedia('(max-width: 767px)').matches`) and applies it to the step controls, the advance control **and** the reference hit areas Story 2.8 added. The flag exists so the player cannot *mutate* the apparatus on a phone; opening a reference mutates nothing — the shelf's own docstring says "Reading here dispatches nothing and changes no progression" — so the suppression is over-broad, and the retired DOM panel that used to provide the fallback is gone. `LibraryRenderer.applyInputState` has no phone check at all, so the two surfaces now disagree about the same rule. Deferred rather than patched because Story 2.12 explicitly owns the sub-768px suppression decision and 2.8's Dev Notes instruct it to preserve today's behaviour. Resolve in one direction when 2.12 owns it — this is the third entry in this file about the same unowned rule (see the 2.6 and 2.7 review items above).
- **`colleagueAnswers` is a single host-level boolean now covering two gates fed by two unrelated hint collections.** `src/adapters/phaser/renderers/advanceView.ts:88-127` — Story 2.8 added `missing-contextual-sources` to `GATE_REFUSAL_CODES`, so `resolveAdvanceRefusal` now routes two codes to the `gate` register while still taking one boolean that says nothing about *which* hint slot is populated. The laboratory passes `selectLocalizedColleagueHint(state) !== undefined`; the library passes `selectLocalizedReadingGateHint(state) !== undefined`. A host that answered `true` from its `colleagueHints` slot would suppress the localized error for a reading-gate refusal and paint the significant-measure line as the answer — a wrong-hint mis-route rather than a silent one. Unreachable today: `missing-contextual-sources` gates only `context → prediction`, and only `LibraryScene` is routed there. But the guarantee is held by phase routing, not by the type, and the docstring's own caveat ("a fact about *this host and this moment*") is an admission. Key the argument to the refusal code rather than to the host the next time this register gains a code.
- **`ReadingRoomDecor` sits in `renderers/` without the renderer contract, and the reading room's fill cost has never been measured.** `src/adapters/phaser/renderers/ReadingRoomDecor.ts` — the class exposes `createRoom(w,h)`, `createCase(w, placements)`, `createDesk(w,h)` and `destroy()`: three constructors, no `create()`, and deliberately no `render(state)`, against project-context §Engine's stated `create()` / `render(state)` / `destroy()` shape for everything in that directory. `destroy()` does release every layer, so the lifetime half of the rule holds and nothing leaks. Separately, the Change Log 1.1 visual rework — which no acceptance criterion covered — adds several hundred fill commands: two wall bays of 30+ generated spines each, a 26-step vignette, two 16-circle radial glows, a 24-slice shelf wash, the wainscot panels and a 20-step desk shadow. Geometry is generated **once** in the create pass and never regenerated, so the "prefer atlases over regenerating `Graphics` each frame" rule is not violated; the open question is draw-call and fill-rate cost against NFR1 (60 FPS at 1280×720 on a low-end school laptop). The recorded manual check covered legibility, truncation and reduced motion, but no frame rate. Measure on target hardware before the next scenery-heavy room (2.9 stages characters in this same room), and bake to a texture with `generateTexture` if it does not hold.
- **The ineligible-artifact context dead end, deferred to Story 3.1 by review decision 2026-08-07.** `src/domain/cases/contextPredictionReadiness.ts:18-19` counts an artifact as missing if it is *ineligible or uninspected*, and `isSourceEligibleForInspection` is `rightsStatus === 'reviewed'` — so an artifact authored as `incomplete` or `unavailable` makes context readiness permanently incomplete, with `reduceSourceInspection` refusing `source-not-eligible` so no surface can ever clear it. Story 2.8 made this visible rather than causing it: the reading room is now the sole surface, and 2.8's authored `missing-artifact` reading-gate hint names the artifact in prose, so the room tells the player the artifact "cannot be read here or cited as evidence" while the colleague asks them to go and read it — two authored strings contradicting each other in an unbreakable loop. `tests/unit/ReadingGateHints.test.ts` pins the looping behaviour as *correct* at the selector layer, which is right for the selector and wrong for the player. Unreachable with shipped Young content (both artifacts are `reviewed`). Deferred because nothing in 2.8 creates or removes it. **Fold into Story 3.1's contract hardening together with the sibling gap closed in 2.8's review** — a `reviewed` artifact with no `textualRendition`, which the schema also permitted and which 2.8 closes by requiring the rendition. Both are the same shape: authorable content that makes a gate unsatisfiable, with nothing detecting "readiness can never become ready".

## Assigned at code review of 2-8-library-reading-room-and-reference-book (2026-08-07)

- **~~Two intents in the `review → debrief` chain have no owner.~~ ASSIGNED to Story 2.11 by review decision 2026-08-07.** `theory.supportRunSelected` / `theory.supportSourceSelected` (`src/ui/theory/TheoryBoard.ts`) and `peerReview.requested` / `revision.saved` (`src/ui/review/ConclusionReviewPanel.ts`) are dispatchable only from retired DOM panels. Story 2.11 already owns the far end of that chain, so it takes these too rather than a new 2.13 being scheduled. **They must land before Story 2.12**, whose completion check is "every player intent is dispatchable from the canvas" and which deletes their only dispatchers — the gap becomes an unreachable `review → debrief` the moment 2.12 lands otherwise. Carried unowned through the 2.7 development notes and the 2.7 review; this is the third time it has been raised, which is why it was assigned rather than deferred again. `tests/e2e/canvas-transitions.spec.ts`'s header table still lists them as unowned and should be updated by 2.11.

## Deferred from: code review of 2-9-colleague-and-rival-lab-characters (2026-08-07)

- **`cardGeometry` has no clamp — a case with a different proposal count pushes cards off the top of the canvas and zeroes the room.** `src/adapters/phaser/renderers/ColleagueRenderer.ts:755-760`. The inverted layout anchors the card block to the canvas floor at a fixed per-board card height, with no bound on `top`: seven conclusion proposals give `768 − 16 − (7×116 + 6×10) = −120`, so the first card is drawn 120px above the canvas — invisible and unclickable — and `proposalStageBand` returns `height: 0`, taking the room and the whole cast with it. Unreachable today: `src/schemas/CaseDefinitionSchema.ts:512-513` pins both proposal arrays at `.length(4)`. The deleted `MIN_CARD_HEIGHT` clamp bounded the analogous failure in the old layout; the rewrite removed the clamp without replacing the bound. Add a floor on `top` if a case ever authors a different count.
- **~~`CharacterStage.create()` runs once per scene, so the theory board's cast never rebuilds across `synthesis → review`.~~ CLOSED during the same review (2026-08-07).** The fix fell out of the locale patch: `CharacterStage.render` now takes the cast on every render and rebuilds its figures when the *set* of people changes, so a `review` beat spoken by someone outside the conclusion proposers is staged correctly. Original finding follows. `src/adapters/phaser/renderers/ColleagueRenderer.ts:464-465`. `stageCast()` derives presence from `selectCasePhase(state)` at *create* time, and `SceneRouter` does not restart a scene whose key is unchanged, so `TheoryBoardScene` builds its cast once and carries it through both phases. A `review` beat spoken by a colleague who authored no conclusion proposal would never be drawn and could never be foregrounded — the conversation would play with nobody staged. Not observable with shipped content (every review speaker is also a conclusion proposer). `CharacterStage.create`'s docstring justifies writing the name and role once on the grounds that "the owner rebuilds this stage when the cast changes"; the owner does not. **Story 3.4** lands the authored `scenarioScript.scenes[].cast` that makes the derived set genuinely vary — resolve it there.
- **`FIGURE_SLOT_WIDTH` in the typography spec is derived from a different count than the renderer uses.** `tests/e2e/french-typography.spec.ts:120-124`. The spec divides the proposal surface by `colleagues.length`; the renderer divides it by `presentColleagueIds(...).length`. Equal for the shipped Young case, where the proposers, the beat speakers and the full cast are the same four people — and divergent the moment Story 3.4 lands the authored per-scene cast, which is the exact change `presentColleagueIds` exists to accommodate. At that point the spec measures a plaque slot nothing paints, which is the failure mode this file has already been patched for twice.

- **~~`canvas-transitions.spec.ts` exceeds its 30s budget at the default `--workers=9`.~~ CLOSED by Story 2.10 (2026-08-07), decided three ways because the measurement showed it was three problems wearing one name.** Re-measured at HEAD after 2.10 added two ~2.4s run animations, a notebook overlay and a typed note to the same walk. **(1) Most of it was not budget at all.** The canvas walks' waits were calibrated in *frames* — Phaser processes pointer input once per rendered frame — and nine concurrent browsers stretch a frame past every fixed timeout tuned at five. The symptom was a drag arriving at a knob whose hit area had not been handed back after a run, arming nothing, so the walk recorded its second observation at the *same* setting and failed three steps later at a scene transition with a routing error pointing nowhere near the cause. `canvasHelpers.dragDesignUntil` replaces those waits with a bounded retry on the value the knob drives, and the walks now assert the throw actually moved, so a lost drag fails where it happens. **(2) A genuine budget raise, derived rather than rounded.** `canvas-transitions.spec.ts` sets its own `test.setTimeout(30_000 + 4 * RUN_STEP_COST_MS)` — the default plus what its two runs actually cost, read from the renderer that runs them, so a change to the animation moves the budget with it. **(3) The workers are capped at 5 in `playwright.config.ts`**, because what was left after (1) and (2) is contention, and contention is a suite-level fact. Measured: at 5 workers, **53 passed / 7 failed, three consecutive runs identical, 1.7–1.8 min**; at 9, 52–53 / 7–8 with `canvas-transitions` intermittent, 1.4 min. Twenty-odd seconds buys a release gate that gives the same answer twice. Splitting the spec was the third option offered and is the wrong one — the single continuous walk *is* the property it asserts. The seven remaining failures are the long-standing retired-DOM names Story 2.12 owns.

## Deferred from: development of 2-10-physical-apparatus-and-player-started-light (2026-08-07)

- **`apparatus.reset` still has no canvas dispatcher and no owner** (raised by the story's own open question 6, not built here). Its only dispatcher is "Reset apparatus" in `src/ui/apparatus/ApparatusControls.ts`, which **Story 2.12 deletes**. It is not in Story 2.10's AC1–AC10 and is not required to complete a case, so it is not an ADR-011 blocker — but 2.12 should inherit a decision rather than a surprise. Three options: give the bench a reset control, assign it to 2.12 with the panel deletion, or declare the intent retired and remove it from `AppAction`. **Assign it before 2.12 starts.**
- **A dead-key accent in the bench notebook's comparison note inserts the unaccented letter** (D5's stated limitation, accepted deliberately). The note is a Phaser `Text` fed by `scene.input.keyboard`, because a DOM `<input>` over the canvas is what §Engine forbids. Typing `´` then `e` on a US-International layout arrives as a `Dead` keydown followed by the base letter, so it writes `e` rather than `é`. An AZERTY keyboard — the realistic French player's — carries `é è à ç ù` as dedicated keys and is unaffected. If it turns out to matter, the fix is a composition-aware handler (`compositionend`), not a DOM element, and it is a follow-up with a named cost rather than a silent gap.
- **The bench has no décor**, and now that the instruments have landed it can be judged. Story 2.9 offered to paint the laboratory and Story 2.10 declined on scope grounds; `LaboratoryDecor` composes into a *band above cards*, which the laboratory does not have, so this is a new composition rather than a reuse. Screenshots at 1280×720 are in the story's Completion Notes — the bench reads as an instrument bench rather than as a bare surface, so this is a polish item and not a defect.
- **`accessible-control.spec.ts` still asserts the retired DOM-parity contract.** Its click target was repointed at the new step affordance in this story (it was restating `(540, 603)` and a private `1024`/`768` pair, which stopped describing anything), so it passes again — but what it asserts is "a canvas gesture is mirrored by a semantic HTML control", which ADR-001 v1.1 retired on 2026-08-05. **Story 2.12 deletes the panel it observes**; that spec needs deleting or rewriting in the same change, and the story's own Dev Notes already name it.
- **`src/ui/apparatus/ApparatusControls.ts` and `src/ui/notebook/NotebookPanel.ts` are now fully superseded on the canvas.** Every intent they dispatch has a bench surface as of this story. They remain mounted and are the *only* readable projection several e2e specs have (canvas text cannot be read from the DOM), which is why four assertions in `young-canvas-experiment.spec.ts` and `canvas-transitions.spec.ts` observe them. **Story 2.12 must replace those observations, not merely delete the panels** — the scene transitions survive as proof, the observation counts do not.

## Deferred from: code review of 2-10-physical-apparatus-and-player-started-light (2026-08-07)

- **`selectAdvancedWavelengthUnlocked` is a second copy of the reducer's wavelength gate, with `550` written down.** `src/core/store/selectors.ts:170-172` filters `run.modelInputs?.wavelengthMode === 'minimum' && run.modelInputs.wavelengthNm === 550` — a character-for-character copy of the private `minimumPathRunCount` at `src/core/store/AppState.ts:331-333`. The selector's own docstring argues that "a surface that counted qualifying runs itself would be a second copy of a gate that already exists and could drift from it", and then is that second copy, moved from the renderer into the store rather than eliminated. Task 5 bullet 1 says to read the authored values from the case, and `comparison.fixedMinimumPathNm` is available — the same file reads it ten lines above. **Deferred rather than patched because the fix has to change both halves at once** and `AppState.ts` is on Story 2.10's do-not-touch list. The two cannot disagree today, and `tests/integration/YoungExperimentBench.test.ts` pins the selector against the reducer's refusal at both 0 and 2 qualifying runs, so a drift is caught at the two interesting points. The real exposure is a future case authoring `fixedMinimumPathNm ≠ 550`: both halves then count zero qualifying runs forever, the chooser paints the advanced choices permanently locked, and no test sees it because every test reads the shipped case where the value happens to be 550. Resolve by parameterising both together, in whichever story next owns `AppState.ts`.
- **The bench renderer has no runtime guard for degenerate authored primary controls.** `src/adapters/phaser/renderers/ApparatusRenderer.ts` / `apparatusGeometry.ts`. Three controls would place instrument slot 2 at x 404–572, straight through the wavelength chooser at x 410–660; a control with `step: 0` would make `knobStepCount` return `Infinity` into `Array.from({ length: Infinity + 1 })` inside `create()` — and a throw in `create()` lands inside `dispatch() → notify()`, which breaks the `Result` contract and strands the router mid-transition (the 1.10 review's failure mode). Unreachable today: `src/schemas/CaseDefinitionSchema.ts:475` types `primaryControls` as a `z.tuple` of exactly two, and the top-level `superRefine` requires both `slitSpacingMm` and `screenDistanceM` with exact `min`/`max`/`step`, so zero, one, three, duplicate ids, `min === max` and `step === 0` are all rejected at authoring time. Worth recording because the guarantee is held by the schema rather than by the surface, and the geometry test's all-pairs collision sweep would catch the three-control case only once someone authored it. Revisit when a case first authors a control set of a different shape.

## Assigned at code review of 2-10-physical-apparatus-and-player-started-light (2026-08-07)

- **~~The NFR1 10-minute re-profile is Blocked with no owner and no reachable due date.~~ ASSIGNED to Alexis Kartmann, due before Story 2.12 is marked done.** `docs/validation/young-performance-2026-08-07.md` recorded "QA / release lead: **Unassigned**", "Remediation owner if blocked: **Unassigned**", and a follow-up date of *"before the Story 2.4 release gate is re-run"* — which is **circular**, because 2.4's gate is itself blocked until Story 2.12 ships, so the follow-up could never have come due. Filing the gate as Blocked rather than inventing a number was correct and the template demands it; leaving it unowned with an unreachable trigger was the defect. Now owned, with the trigger moved to **before Story 2.12 is marked done** — 2.12 retires the DOM panels and closes Epic 2, so it is the last point at which a bench performance regression is cheap to find. Reassign the owner if someone else takes the hardware; do not clear the trigger. **Separately, AC10's last clause was corrected**: it asked for the profile "with drag, staging, and propagation all active", and character staging cannot occur at the bench — Story 2.9's Dev Notes and Story 2.10's own scope boundary both forbid it, so the clause named a configuration the laboratory cannot enter. It now names what the bench actually runs under load (drag, propagation, the notebook overlay, the wavelength chooser). The gate is unchanged in substance: 10 minutes, 1280×720, representative low-end hardware, manually observed, no automated figure substituted.

## Deferred from: code review of 2-11-debrief-scene-and-replay (2026-08-07)

- **`tests/unit/sceneSlice.ts` reports a constant `height: 18` for every text object, and stubs `setFontSize` / `setCrop` permissively.** `tests/unit/sceneSlice.ts:235`. Already recorded by the developer under §Carried forward; this review promotes it, because it is the single highest-value item the 2.11 review produced. The harness cannot distinguish a text placed against a **measured** neighbour from one placed against a **constant**, and it cannot reach either new renderer's clamp shrink loop, its crop branch, or its `available <= 0` branch. Sixteen of this review's findings are text drawn into a reserve that cannot hold it, and every one of them shipped under a 1125-test green suite for exactly this reason — the same shape as 2.10's `const dark = false` and doubled `step()`, which left 982/982 green. `french-typography.spec.ts` covers fixed-height *control labels* only, so no multi-line reserve in either new surface is checked anywhere. The fix is to derive the fake's height from the wrapped text and font size, and it is a change to a harness three suites share, so it belongs to its own pass. **Until it lands, every "the band does not overlap" and "the text fits" claim in a Phaser renderer is an assertion about arithmetic, not about what is painted** — which is why the story demands screenshots, and why the two defects the 1280×720 pass caught were caught by eye.
- **`CASE_FILE_READINESS_ROWS = 11` silently drops a twelfth missing requirement.** `src/adapters/phaser/renderers/caseFileGeometry.ts:117`; `CaseFilePresenter.renderReadiness` iterates the eleven reserved rows rather than the `missing` array, so a code past the eleventh is dropped with no overflow line and no indication. Reserving the whole enum was the right call and its docstring says why — the exposure is that nothing verifies 11 *is* the enum's size: `tests/unit/I18n.test.ts` concedes `MissingConclusionRequirementCode` is "a type union with no runtime counterpart, so the roster cannot be swept". A twelfth code compiles, ships, and hides a requirement AC7 promised to show. Close it with a type-level exhaustiveness check against the union, or an overflow line when `missing.length > CASE_FILE_READINESS_ROWS`. Same class as the eight dead `recognition.*` keys the 2.10 review patched: a roster that cannot be swept drifts silently.
- **The canvas e2e walks are load-sensitive, and 2.11's walk got longer without its budget moving.** `tests/e2e/canvasHelpers.ts:349`. The contention class is pre-existing and is documented at length in `playwright.config.ts` (the 2.10 decision that capped `workers: 5`); this story amplified it rather than caused it. The claimed **53 passed / 7 failed** does reproduce on an idle machine — confirmed against baseline `3bd19b7` and at HEAD — but under CPU load `canvas-transitions.spec.ts:102`, `debrief-replay.spec.ts:54` and `young-canvas-experiment.spec.ts` drop out with `expectActiveScene` timing out at `canvasHelpers.ts:167`, and all three pass in isolation at 17.4s and 17.5s against a 30s budget. That is thin headroom for a release gate whose config comment claims "three consecutive runs identical". Meanwhile `WALK_TO_DEBRIEF_COST_MS` is still `4 * RUN_STEP_COST_MS`, byte-identical to the pre-2.11 value, while the header edited in the same commit says "Story 2.11 added a case file opened twice. It does more, so it takes longer… Derived rather than rounded." Re-derive the constant to include the two overlay open/close cycles and the six `waitForInputToSettle` pauses, so a timeout under load reads as a budget fact rather than a product defect. **Do not raise `workers`** — that decision was measured and is not this item's to reopen.
